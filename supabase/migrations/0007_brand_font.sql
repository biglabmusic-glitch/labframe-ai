-- Шрифт подписи бренда: юзер выбирает один из набора предустановленных шрифтов
-- (см. app/src/lib/fonts.ts). AI-агент учитывает это поле в промте для генерации,
-- а на фронте подпись/имя мастера в превью рендерится этим шрифтом.
alter table public.brand
  add column if not exists font_id text;

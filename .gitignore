node_modules
dist
dist-types
dist-ssr
build
.vercel
.next
.cache

# TypeScript incremental build artifacts
*.tsbuildinfo

# env
.env
.env.*
!.env.example

# logs / OS
*.log
.DS_Store
Thumbs.db

# IDE
.vscode/*
!.vscode/extensions.json
.idea/

# Supabase CLI local state
supabase/.branches
supabase/.temp

# Бэкапы данных — содержат персональные данные (first_name, last_name, photo_url).
# Скрипт кладёт их вне репозитория, но подстрахуемся на случай запуска с -OutDir внутрь.
labframe-backups/
data_*.sql

/**
 * Временные фичфлаги pre-release.
 */

/**
 * Когда true — лимиты генераций не отображаются юзеру (показываем «Без ограничений»),
 * кнопка «Улучшить» скрыта.
 *
 * Значение берётся из env, чтобы включение оплаты не требовало правки кода и релиза:
 * по умолчанию (переменная не задана) флаг ВКЛЮЧЕН, как и было захардкожено раньше.
 *
 * Включение оплаты — две переменные с одинаковым смыслом:
 *   Vercel:   VITE_LIMITS_DISABLED=0  — начинаем показывать лимиты в UI
 *   Supabase: LIMITS_DISABLED=0       — бэк начинает их применять (create-job)
 */
export const LIMITS_DISABLED = (import.meta.env.VITE_LIMITS_DISABLED ?? '1') !== '0';

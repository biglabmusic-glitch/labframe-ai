/**
 * Временные фичфлаги pre-release. Когда соответствующая фича готова
 * к включению — поменять значение здесь (или вытащить в import.meta.env).
 */

/**
 * Когда true — лимиты генераций не отображаются юзеру (показываем «Без ограничений»),
 * кнопка «Улучшить» скрыта. БД-триггер enforce_usage_limit также снят миграцией
 * 0005_disable_usage_limit.sql. Вернуть в false когда подключим оплату
 * (Telegram Stars / ЮKassa).
 */
export const LIMITS_DISABLED = true;
